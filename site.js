const preview=new URLSearchParams(location.search).get("preview")==="1",source=preview?"content/draft.json":"content/site.json";
const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c])),br=v=>esc(v).replace(/\n/g,"<br>");
const yt=v=>{try{const u=new URL(v);if(u.hostname.includes("youtu.be"))return u.pathname.slice(1).split("/")[0];if(u.hostname.includes("youtube.com"))return u.searchParams.get("v")||u.pathname.split("/").filter(Boolean).pop()}catch(_){}return""};
if(preview){document.getElementById("previewBadge").hidden=false;document.querySelectorAll("a.local").forEach(a=>a.href+=(a.href.includes("?")?"&":"?")+"preview=1")}
fetch(source+"?v="+Date.now(),{cache:"no-store"}).then(r=>r.ok?r.json():Promise.reject()).then(data=>{document.querySelectorAll(".site-logo").forEach(i=>i.src=data.logo||"media/logo.png");const page=document.body.dataset.page;if(page==="home")renderHome(data);else renderWorks(data,page)}).catch(()=>{const r=document.getElementById("items");if(r)r.innerHTML='<div class="empty">作品データを読み込めませんでした。</div>'});
function renderHome(d){document.title=`${d.title}｜旅、写真、記録。`;q("title").textContent=d.title;q("reading").textContent=d.reading;q("tagline").innerHTML=br(d.tagline);q("heroMain").style.backgroundImage=`url('${esc(d.hero_image)}')`;q("heroSub").style.backgroundImage=`url('${esc(d.hero_sub_image)}')`;q("feature").textContent=d.feature_caption;q("aboutTitle").innerHTML=br(d.about_title);q("aboutBody").textContent=d.about_body;q("footerText").textContent=d.footer_tagline;const works=(d.works||[]).slice(0,3);q("cards").innerHTML=works.map(w=>`<article class="card"><div class="card-media" style="background-image:url('${esc(w.image)}')"></div><h3>${esc(w.title)}</h3><em>${esc(w.type)}</em><p>${esc(w.note)}</p></article>`).join("")}
function renderWorks(d,page){let works=d.works||[];works=page==="gallery"?works.filter(w=>!String(w.type).toLowerCase().includes("film")):works.filter(w=>yt(w.youtube_url)||String(w.type).toLowerCase().includes("film"));const root=q("items");if(!works.length){root.innerHTML=`<div class="empty">まだ${page==="gallery"?"写真":"動画"}作品がありません。<br>編集画面から追加するとここへ表示されます。</div>`;return}root.innerHTML=works.map(w=>{const id=yt(w.youtube_url),media=page==="films"&&id?`<div class="video"><iframe src="https://www.youtube-nocookie.com/embed/${encodeURIComponent(id)}" title="${esc(w.title)}" loading="lazy" allowfullscreen></iframe></div>`:`<div class="work-image" style="background-image:url('${esc(w.image)}')"></div>`;return`<article class="work">${media}<h2>${esc(w.title)}</h2><p>${esc(w.note)}</p></article>`}).join("")}
function q(id){return document.getElementById(id)}
